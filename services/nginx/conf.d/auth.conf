server {
    listen 80;
    server_name auth.invariantcontinuum.io;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name auth.invariantcontinuum.io;

    ssl_certificate /etc/letsencrypt/live/auth.invariantcontinuum.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/auth.invariantcontinuum.io/privkey.pem;
    include /etc/nginx/snippets/ssl.conf;

    location = /healthz {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location / {
        proxy_pass http://keycloak:8080;
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
        include /etc/nginx/snippets/proxy.conf;
    }
}
