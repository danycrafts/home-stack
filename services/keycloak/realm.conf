# Mounted Keycloak realm configuration placeholder.
# The stack starts with an empty Keycloak instance. Put importable realm JSON
# files in services/keycloak/import/ when a realm should be loaded manually.
