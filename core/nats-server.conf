# NATS server config mounted into each cluster node.
# Non-clustering options live here; node-specific cluster routes stay as CLI flags.
max_payload: 16MB
jetstream {
  store_dir: "/data/jetstream"
}
